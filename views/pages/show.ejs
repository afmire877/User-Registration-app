<!DOCTYPE html>
<html>
<head>
	<title></title>
		<% include ../partials/head %>
</head>
<body>
		<% include ../partials/header %>
		<h1>This is all the Information on <%= user.name %> </h1>
		<table class="table">
<!-- 			<td><img src="assets/img/profile-placeholder.jpg"></td>
 -->			<tr>

				<td scope="row">Name:</td>
				<td><%= user.name %></td>
			</tr>
			<tr>
				<td scope="row">Email:</td>
				<td><%= user.email %></td>
			</tr>
			<tr>
				<td scope="row" >Gender:  </td>
				<td > <%= user.gender %></td>
			</tr>
			<tr>
				<%  var date = new Date(user.DOB); 
					var m = date.getMonth();
					var d = date.getDate();
					var y = date.getFullYear();
				%>
				<td>Date of Birth:</td>
				<td><%= d + "/" + m + "/" + y %></td>
			</tr>
		</table>

<footer>
		<% include ../partials/footer %>
</footer>
</body>
</html>